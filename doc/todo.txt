发现问题：
1.工业部门信息表中是用保障部队编号还是名称，或者都有？（暂时设置为名称）
2.工业部门信息表中缺少维修人员字段（lxd已解决）
3.维修中心格式确认中缺少确认按钮，或其它确认方式？（lxd已解决）
4.审核后，需要将审核状态改为1/2，而不是插入一条新的审核记录，并且审核窗口应该隐藏（lxd已解决）
5.防止重复审核--审核后，需要重设状态并将审核按钮设置为不可点击状态；（lxd已解决）
6.二级审核完毕后，step应该怎么设置，现在进度条上还是二级待审核（lxd已解决）
7.工业部门信息提交失败(已解决--servlet没有继承http)
8.分发人员登录后，查询到的故障单记录上没有处理按钮（lxd已解决）
9.分发成功后，弹出的对话框怎么隐藏掉（lxd已解决）
10.在工业部门登录并查询到记录后，需要在后面添加处理按钮，直接获取已有的信息(比如故障单号等),（lxd已解决）
另外在处理的逻辑上，应该是先进行反馈表单的填写和入库，然后根据反馈表单生成的ID号和维修手段来判断是否需要生成远程支援信息表；
11.登录页面上，如果登录不成功，返回提示信息（lxd已解决）
12.故障单详情查询中的step是如何定义的？现在step=0时，到故障单二级审核了，而且后面没有到的步骤在状态图上没有显示（lxd已解决）
13.对于维修中心非分发人员来说，查询到的每条记录后面应该加上一个按钮，进行格式审查提交或者电话回访确认；（lxd已解决）
14.对于前后端交互的时间格式，统一规定为"YYYYMMDDHHMMSS"的字符串；
15.故障单搜索返回类型如何定义？
16.统计图表的数据接口？
17.用户管理模块中，必须提供添加下级帐号的功能，帐号信息如下：（lxd已解决）
   用户名
   类型(根据当前用户类型进行获取)
   全名
   工作单位/部门
                  维修中心(格式化检查/分发人员)：军区/专家/工业部门
                  军区：集团军
                  集团军：团级
18.本地文件导入功能：
   导入本地的故障文件（xml格式），进行本地导入。
   需要在页面上提供一个本地导入按钮，点击后先弹出一个本地文件导入框，然后获取本地文件，导入到故障表单中；


剩余工作：
1.美化
2.故障表单的手动录入测试
3.工业部门的手动录入测试
4.专家信息的手动录入测试
5.图片的上传和存储设计和实施
6.语音记录的存储设计和实施
7.日志信息表格的设计和实施
8.搜索模块的测试

9.格式检查里面，两个按钮变成了上下层叠；
10.分发的时候，获取工业部门目前最多只有一个，后面需要将这个接口修改，如果只查到一个工业部门，点击按钮直接分发，
  如果多个，提示用户选择，选择后分发；
11.需要将工业部门用户登录时查询用户单位的servlet屏蔽掉，另外一个需要屏蔽的就是专家；
12.反馈表中，默认现场维修勾选
13.缺少专家信息的录入；


关于统计的一点思考：
1.统计部分目前查询的因子有：
  开始/结束时间		用户所属单位		装备类型/名称/编号		关键字
  开始/结束时间：根据选择的时间阶段来计算开始/结束时间
  用户所属单位：需要先根据用户的类型查询当前级别所有单位分类，然后统计每类单位下的用户处理的故障单个数，再求和；
  装备类型/名称/编号：只选择一个输入

  需要查询所有用户单位分类型


0427:
-1.	故障维修信息查询添加单据查询、上报人员查询，
	区域管理范围查询添加所有隶属下级，
	能够对故障发生部位（或关键字，客户要求满足一项即可）的查询；
-2.	接线员与T级人员语音录音提示路径，T级上报时候能够上传离线语音；
-3.	团级能够在与接线员通话时，填写上报故障单；
-4.	工业部门与专家语音录音提示路径；
-5.	统计展示能够由折线图切换至柱状图；
-6.	对于上报数据提交，都进行完成与否提示。
-7.	审批同意与否都进行同意意见描述。
-8.	中心进行统计和查询后，都可以进行数据打包，存成xxx.dmp格式。
9.	对所有查询，统计的表单数据，能够进行打印。

0505:
1.针对统计，故障统计表意不明确；
2.已处理表单-本月（数据编制）
3.统计结果如果为0，输入边界，可进行友好提示。
4.统计展示中，建议区分已处理和未处理数据信息。
5.统计展示中，输入框的提示，引导用户按照规则使用。
6.统计图示中，多条曲线的对比示意。
7.自动学习装备分类。
8.列表的单号显示。
-9.关键字-"故障现象关键字"
-10.搜索的焦点更替。
-11.语音G格A标识
12.团级业务数据等级划分，同级数据不共享。
13.职能类别规范，
14.调试数据在每个域多生成一类。

0506:
-1.操作完成后，表格刷新被304
2.详情中，部分数据未返回
  "dpid":"","dpunit":"","dptime":"","dpsafeunit":"","dpaddr":"","dpcontact":"",
  "fbrpbjname":"","fbrpbjtype":"",
  "rsexpert":"","rscontacter":"","rscontactway":""
3.离线语音处理后，对应的团级的未处理单子看不到




